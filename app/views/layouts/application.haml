!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}/
    %title 
      1000+ startup ideas from the crowd
    = favicon_link_tag 'doesmystartupideasuck-logo.png'
    %meta{:content => "#{content_for?(:description) ? yield(:description) : "Doesmystartupideasuck"}", :name => "description"}/
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => 'reload'
    = javascript_include_tag 'application', 'data-turbolinks-track' => 'reload'
    = csrf_meta_tags
  %body
    = render 'layouts/navbar'
    .container
      %br
      = render 'layouts/messages'
      .row
        .col
          = yield
        .col-md-auto
          - if current_user
            = render 'layouts/sidebar'
          - else
            = link_to 'Sign in', new_user_session_path, class: 'btn btn-outline-success'
            = link_to 'Sign up', new_user_registration_path, class: 'btn btn-outline-success'
            %p
            = link_to "Sign in with Facebook", user_facebook_omniauth_authorize_path, class: 'btn btn-outline-primary button'
